# Remotion compatibility regression

These tests verify that producer-only fields are removed, approved narration is unchanged, explicit Financial Visual bindings cover every selected financial template, and Visual Grammar fields are flattened for render_spec 2.4.0. The GitHub Actions daily workflow additionally runs the pinned Remotion repository's official validator.
